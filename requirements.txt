# Serverless-Market-Data-Ledger — Python dependencies
#
# Bounds are conservative: lower = a broadly-available stable release the ported
# code's APIs are compatible with; upper = the next breaking major.
# The trailing comment on each line is the exact version verified against the
# Scout Sniper source venv (2026-07). Relax/tighten after your first clean run.

aiohttp>=3.9,<4        # verified 3.13.3  — bronze/ Dukascopy CDN fetch
pandas>=2.2,<4         # verified 3.0.1   — all layers
pyarrow>=15,<24        # verified 23.0.1  — bronze/ Parquet write
duckdb>=1.0,<2         # verified 1.5.1   — gold/ tick->OHLC resample
ta>=0.11,<0.12         # verified 0.11.0  — gold/ ATR (AverageTrueRange)
boto3>=1.34,<2         # verified 1.43.42 - engine/ R2 (S3-compatible) Bronze upload
requests>=2.31,<3      # verified 2.32.3  - engine/ Cloudflare D1 HTTP API
python-dotenv>=1.0,<2  # verified 1.x     - engine/ local .env loading (optional at runtime)
numpy>=1.26,<3         # verified 2.4.3   — gold/ ATR
